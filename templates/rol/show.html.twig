{% extends 'base.html.twig' %}

{% block title %}Rol{% endblock %}

{% block body %}
<header>
    {{ parent() }}
</header>

<h5 class="m-4">Rol</h5>

    <div class="container-fluid container-sm m-4" style="padding:30px">
    
        <table class="table table-bordered table-sm" style="border: 2px solid #E8550F">
            <tbody>
                <tr>
                    <th style="background-color:#FFA37E">Tipo de Rol</th>
                    <td>{{ rol.title }}</td>
                </tr>
            </tbody>
        </table>

    </div>

    <a href="{{ path('app_rol_index') }}" class="btn btn-md m-4" style="background-color: #020100; color: #ffffff">Volver a la lista</a>

    <a href="{{ path('app_rol_edit', {'id': rol.id}) }}" class="btn btn-md m-4" style="background-color: #020100; color: #ffffff">Editar</a>

    {{ include('rol/_delete_form.html.twig') }}

{% endblock %}
