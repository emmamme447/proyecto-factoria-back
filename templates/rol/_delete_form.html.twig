<form method="post" action="{{ path('app_rol_delete', {'id': rol.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rol.id) }}">
    <button class="btn btn-md" style="background-color:#FE4700"  style="background-color:#FE4700" >Borrar</button>
</form>
