{% extends 'base.html.twig' %}

{% block body %}
    <header>
        {{ parent() }}
    </header>



{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-md" "background-color: #020100; color: #ffffff" >{{ button_label|default('Guardar') }}</button>
{{ form_end(form) }}
{% endblock %}
