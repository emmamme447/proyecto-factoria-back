{% extends 'base.html.twig' %}

{% block title %}Edit Contract{% endblock %}

{% block body %}

<header>
    {{ parent() }}
</header>

<h5 class="m-4">Editar tipo de contrato</h5>

    <div class="m-4" style="border: 2px solid #E8550F">
    
        {{ include('contract/_form.html.twig', {'button_label': 'Guardar'}) }}

    </div>

    <a href="{{ path('app_contract_index') }}" class="btn btn-md m-4" style="background-color: #020100; color: #ffffff">Volver a la lista</a>

    {{ include('contract/_delete_form.html.twig') }}

{% endblock %}
