{% extends 'base.html.twig' %}

{% block title %}Equipos{% endblock %}

{% block body %}

<header>
    {{ parent() }}
</header>

    <h5 class="m-4">Equipos</h5>

    <body >

        <div class="container-fluid" style="padding:30px" >
        
            <table class="table table-bordered" style="border: 2px solid #E8550F">
                <tbody>
                    <tr>
                        <th style="background-color: #FFA37E">Título</th>
                        <td>{{ team.title }}</td>
                    </tr>
                </tbody>
            </table>

        <a href="{{ path('app_team_index') }}" class="btn btn-md" style="background-color: #020100; color: #ffffff" >Volver a la lista</a>

        <a href="{{ path('app_team_edit', {'id': team.id}) }}" class="btn btn-md" style="background-color: #020100; color: #ffffff">Editar</a>

        {{ include('team/_delete_form.html.twig') }}

        </div>

    </body>

{% endblock %}
