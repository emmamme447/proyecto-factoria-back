{% extends 'base.html.twig' %}

{% block title %}Edit Employee{% endblock %}

{% block body %}
    <h1>Edit Employee</h1>

    {{ include('employee/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_employee_index') }}" class="btn btn-outline-primary" style="--bs-btn-padding-y: .15rem; --bs-btn-padding-x: .5rem; --bs-btn-font-size; margin-bottom: 20px">back to list</a>

    {{ include('employee/_delete_form.html.twig') }}
{% endblock %}
