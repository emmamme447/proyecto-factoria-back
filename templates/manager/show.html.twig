{% extends 'base.html.twig' %}

{% block title %}Responsables{% endblock %}

{% block body %}
    <header>
    {{ parent() }}
    </header>

    <h5 class="m-4" >Responsables</h5>

    <div class="container-fluid" style="padding:30px" >
        <div style="display: flex; justify-content: center; margin-left:10%">
            <table class="table table-bordered" style="border: 2px solid #E8550F; width: 50%">
                <tbody>
                    <tr >
                        <th style="background-color: #FFA37E">Nombre</th>
                        <td>{{ manager.title }}</td>
                    </tr>
                    <tr>
                        <th style="background-color: #FFA37E">Nº de identificador</th>
                        <td>{{ manager.identifying }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>       
    <div style="display: flex; justify-content: center; width: 100%">
    <a href="{{ path('app_manager_index') }}" class="btn btn-sm m-2" style="background-color: #020100; color: #ffffff">volver a la lista</a>

    <a href="{{ path('app_manager_edit', {'id': manager.id}) }}" class="btn btn-sm m-2" style="background-color: #020100; color: #ffffff">editar</a>
    </div>

    <div style="justify-content: center; display: flex">
    {{ include('manager/_delete_form.html.twig') }}
    </div>
{% endblock %}
