{% extends 'base.html.twig' %}

{% block title %}Equipos{% endblock %}

{% block body %}
<header>
    {{ parent() }}
</header>

<h5 class="m-4">Equipos</h5>
    <div class="container-fluid container-sm m-4" style="padding:30px, margin-left:20%">
        <div style="display: flex; justify-content: center; margin-left: 10%">
            <table class="table table-bordered " style="border: 2px solid #E8550F; width: 50%">
                <tbody>
                    <tr>
                        <th style="background-color: #FFA37E">Título</th>
                        <td>{{ team.title }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
    <div style="display: flex; justify-content: center; width: 100%">
    <a href="{{ path('app_team_index') }}" class="btn btn-md m-4" style="background-color: #020100; color: #ffffff" >Volver a la lista</a>

    <a href="{{ path('app_team_edit', {'id': team.id}) }}" class="btn btn-md m-4" style="background-color: #020100; color: #ffffff">Editar</a>
    </div>
    <div style="justify-content: center; margin-top:10px; display: flex">
    {{ include('team/_delete_form.html.twig') }}
    </div>
{% endblock %}
