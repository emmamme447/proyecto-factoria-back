{% extends 'base.html.twig' %}

{% block title %}New Employee{% endblock %}

{% block body %}
    <h1>Create new Employee</h1>

    {{ include('employee/_form.html.twig') }}

    <a href="{{ path('app_employee_index') }}" class="btn btn-outline-primary"
    style="--bs-btn-padding-y: .15rem; --bs-btn-padding-x: .5rem; --bs-btn-font-size">back to list</a>
{% endblock %}
